<nav class="navbar navbar-light bg-faded">
  <ul class="nav navbar-nav">
    <% if logged_in? %>
      <li class="nav-item">
        <%= link_to "Ohuse", creators_path, class: 'navbar-brand' %>
      </li>
      <li class="nav-item active">
        <button class="btn btn-outline-primary nav-item" data-toggle="modal"
          data-target="#createTicket">
          Create
        </button>
      </li>
      <li class="nav-item dropdown float-xs-right">
        <a class="nav-link pull-right dropdown-toggle"
          href="" id="navbarDropdownMenuLink"
          data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= image_tag current_user.image_url, :class => "rounded-circle" %>
        </a>
        <div class="dropdown-menu dropdown-adjust" aria-labelledby="navbarDropdownMenuLinkn">
          <%= link_to "Profile", current_user, :class => "dropdown-item" %>
          <% if current_user.admin %>
            <%= link_to "Manage", kannri_path, :class => "dropdown-item"  %>
          <% end %>
          <%= link_to "logout", logout_path, :class => "dropdown-item"  %>
        </div>
        </li>
    <% else %>
      <%= link_to "Log in", '/auth/twitter',
                                        class: 'navbar-brand float-xs-right' %>
    <% end %>
  </ul>
</nav>
